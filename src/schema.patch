diff --git a/src/schema.rs b/src/schema.rs
index d287b1a..a22c674 100644
--- a/src/schema.rs
+++ b/src/schema.rs
@@ -1,39 +1,39 @@
 table! {
     last_tweet (id) {
-        id -> Integer,
+        id -> BigInt,
         status_id -> BigInt,
     }
 }
 
 table! {
     tweets (id) {
-        id -> Integer,
+        id -> BigInt,
         text -> Text,
         user_id -> BigInt,
         in_reply_to_status_id -> Nullable<BigInt>,
         quoted_status_id -> Nullable<BigInt>,
     }
 }
 
 table! {
     twitter_tokens (id) {
-        id -> Integer,
+        id -> BigInt,
         access_token -> Text,
         access_token_secret -> Text,
     }
 }
 
 table! {
     websub_active_subscriptions (id) {
-        id -> Integer,
+        id -> BigInt,
         expires_at -> BigInt,
     }
 }
 
 table! {
     websub_pending_subscriptions (id) {
-        id -> Integer,
+        id -> BigInt,
         created_at -> BigInt,
     }
 }
 
@@ -41,9 +41,9 @@
 table! {
     websub_subscriptions (id) {
-        id -> Integer,
+        id -> BigInt,
         hub -> Text,
         topic -> Text,
         secret -> Text,
     }
 }
 
